# Checkov configuration for the Day 21 review.
#
# The organization's five infrastructure requirements map to a small set of
# checks. Two additional checks are out of scope for this review, so we skip
# them with the reasons below. Skipping a check is a recorded team decision,
# not a way to hide a failure.

skip-check:
  # Storage queue logging is not part of this runbook review. The organization
  # covers it in a separate logging standard.
  - CKV_AZURE_33
  # Customer-managed keys require a Key Vault that this proposed change does not
  # yet include. Key management is reviewed in a later hardening task.
  - CKV_AZURE_100
