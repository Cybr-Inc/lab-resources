# Checkov configuration for the Day 21 review.
#
# This exercise reviews Cosmos DB access controls, region, and role scope.
# Cosmos DB encrypts data at rest with Microsoft-managed keys by default.
# Customer-managed keys are a separate key-management requirement.

skip-check:
  # This exercise uses default encryption, not customer-managed keys.
  - CKV_AZURE_100
